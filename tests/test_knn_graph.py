from seesaw.knn_graph import *


def test_get_matrix():
    # check that matrix sums match degree when the kernel radius is large, 1 when it is tiny.
    # check that matrix is diagonal ones when k is given as 1
    pass